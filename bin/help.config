#!/usr/bin/env bash

cat <<'EOF'
This plugin currently builds Lisette from source.

Required tools:
- Cargo/Rust toolchain
- Go 1.25 or newer

Optional environment variables:
- GITHUB_API_TOKEN: used for authenticated GitHub API requests when resolving versions in CI or other rate-limited environments.
EOF
